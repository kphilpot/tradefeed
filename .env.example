# TradeFeed Environment Variables
# Copy this file to .env and fill in your values.
# Never commit .env to version control.

# ─── Supabase ──────────────────────────────────────────────
# Get these from your project dashboard → Settings → API
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key-here

# ─── Stripe (Phase 3 — payments) ───────────────────────────
# Public key is safe to expose to the browser
VITE_STRIPE_PUBLIC_KEY=pk_live_...
# Create a $20/mo recurring price in Stripe Dashboard and paste the price ID
VITE_STRIPE_PRICE_ID=price_...

# The following are server-side only — set them as Supabase Edge Function secrets
# (Dashboard → Settings → Edge Functions → Secrets), NOT in this file:
#   STRIPE_SECRET_KEY=sk_live_...
#   STRIPE_WEBHOOK_SECRET=whsec_...   (Stripe Dashboard → Webhooks → signing secret)

# ─── Resend (Phase 3 — newsletter email delivery) ───────────
# Also set as Supabase Edge Function secrets, not here:
#   RESEND_API_KEY=re_...
#   FROM_EMAIL=newsletter@tradefeed.io
#   SITE_URL=https://tradefeed.io

# ─── App config ────────────────────────────────────────────
VITE_APP_NAME=TradeFeed
VITE_APP_ENV=development
